package com.examora.backend.exam.entity;

/**
 * Defines how exam is structured.
 */
public enum ExamMode {
    FULL, // Single flow
    SECTION, // Section-wise navigation
    SUBSECTION // Subsection-wise navigation
}